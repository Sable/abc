aspect ParserTraceAspect {
  class ParserTrace {
    /* Flag for personalized parser trace. */
    public static boolean parserTraceOn = false;

    public static void parserTrace (String s)
      { if (parserTraceOn) 
          System.err.println(" REDUCED: " + s); 
      }
  }
}
