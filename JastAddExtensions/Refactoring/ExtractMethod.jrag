aspect ExtractMethod {
  public void Block.extractMethod(String visibility, String name, int begin, int end) throws RefactoringException {
    extractBlock(begin, end).wrapIntoClosure().convert().extractToMethod(visibility, name);
  }
}